# 内容编写指南（AUTHORING）

RustWay 的核心设计：**内容即数据，加载器自动发现**。添加/插入知识点不需要改任何 Rust 或前端代码。

## 快速开始（3 分钟）

```bash
cp content/kps/_TEMPLATE.json content/kps/my-topic.json   # 1. 复制模板（去掉下划线前缀）
$EDITOR content/kps/my-topic.json                          # 2. 填写知识点
cargo run -- validate                                      # 3. 本地校验（秒级反馈）
cargo test                                                 # 4. 全量测试（CI 同款门禁）
```

> **草稿机制**：以下划线 `_` 开头的文件（如 `_TEMPLATE.json`、`_draft-v2.json`）不会被加载——可以安心在仓库里存放半成品。

## 文件布局

```text
content/
├── curriculum.json    # 课程结构：模块 → 章节 → 关卡测验；章节的 kps 数组顺序 = 学习路径
└── kps/*.json         # 知识点定义。⚠️ 目录下所有非 _ 开头的 .json 会被自动合并 —— 随便建新文件
```

## 两种插入方式

**方式 A：全新文件（推荐，零冲突）**
新建 `content/kps/whatever-name.json`，顶层结构 `{"kps": [ {...}, {...} ]}`。保存即入库。

**方式 B：追加进已有文件**
把新知识点对象追加进任意已有 `kps/*.json` 的 `kps` 数组（位置无所谓，id 唯一即可）。

**编入章节（可选）**
在 `curriculum.json` 中，把知识点 `id` 写进目标章节的 `"kps": [...]` 数组——**放在哪个位置，学习路径就在哪里**。不编入也不丢失：它会出现在网站首页的「待编排」区。

## 知识点 Schema

```jsonc
{
  "id": "k1-3-1",                    // 全局唯一，建议 k<模块><章><序>
  "title": "所有权三规则",
  "difficulty": 2,                   // 1 入门 / 2 中阶 / 3 进阶
  "minutes": 35,                     // 预计学习分钟数
  "summary": "一句话概述（会显示在图谱提示与卡片上）",
  "outline": ["要点一", "要点二", "要点三"],   // 详细文档缺失时的大纲展示
  "prereqs": ["k1-1-3"],             // 前置知识点 id；必须存在，且不允许成环
  "task": "动手验证任务：必须在终端/测试里可客观完成，写清预期结果",
  "refs": [                          // 至少 1 条，必须是 http(s) 链接
    { "t": "The Rust Book 4.1", "u": "https://doc.rust-lang.org/book/ch04-01-understanding-ownership.html" }
  ],
  "quiz": [                          // 至少 1 题；答对率决定「掌握」状态
    {
      "q": "题干",
      "opts": ["选项A", "选项B", "选项C", "选项D"],
      "answer": 1,                   // 正确选项下标（0 起）
      "why": "解析：为什么正确，以及错误选项错在哪"
    }
  ],
  "detail_md": "content/rust/ownership.md",  // 可选；详细讲解 Markdown（相对仓库根）
  "tags": ["所有权", "核心"]
}
```

章节 Schema（在 `curriculum.json` 中）：

```jsonc
{
  "id": "c1-3",
  "title": "所有权系统",
  "objectives": ["学习目标 1", "学习目标 2"],
  "kps": ["k1-3-1", "k1-3-2"],       // 顺序即学习路径，可任意插入
  "checkpoint": [                    // 关卡测验：每题必须用 kp 指回本章知识点
    { "q": "…", "opts": ["…"], "answer": 0, "why": "…", "kp": "k1-3-1" }
  ]
}
```

## 完整性规则（cargo run -- validate / cargo test 自动校验）

**错误（阻塞，CI 拒绝合入）**：

1. 知识点 `id` 全局唯一；一个知识点只允许编入一个章节；模块 `num` 不重复
2. 章节/关卡测验引用的 id 必须存在；关卡测验的 `kp` 必须属于本章
3. `prereqs` 必须存在且前置图无环（Kahn 检测）
4. 每个知识点：摘要、验证任务、≥1 道自测题、≥1 条 http(s) 引用，缺一不可
5. 自测题：选项 ≥2、`answer` 不越界、解析非空
6. `difficulty` 必须是 1/2/3
7. `detail_md` 指向的文件必须存在
8. 课程骨架规模下限（模块数/知识点数/测验题数）不可被意外删减

**警告（不阻塞，建议修复，`validate` 与 `/api/manifest` 可见）**：

- 知识点是知识图谱孤岛（无前置也无后继）
- 摘要超 80 字、minutes 超范围、id 命名不规范
- 无详细文档也无大纲、重复引用、空文档文件
- 章节缺少学习目标

## 质量标准（什么样的知识点算「打磨好」）

- **summary**：独立成句，让读者不看正文也知道学什么
- **task**：可在 30 分钟内完成、有客观验收（测试/命令输出/可复现产物）
- **quiz**：先答后看（题干独立可答）；干扰项有迷惑性；解析说清「为什么错」
- **refs**：优先官方文档 > 权威教材 > 经典论文；链接必须可访问
- **prereqs**：宁多勿漏——图谱的导航价值取决于依赖链的诚实度
- **detail_md**：目标 ≥ 800 字 + 可运行代码 + 常见误区表 + 验证清单（模板见现有 `content/rust/ownership.md`）

## 详细文档模板（detail_md）

```markdown
# <标题>

> 对应知识点：<id> · 预计 N 分钟 · 难度 ★☆☆

## 学习目标
- …

## 一、<概念正文>
（原理 + 示例代码 + 表格对比；代码示例必须可复制运行）

## 常见误区
| 误区 | 事实 |

## 验证清单
- [ ] …

## 延伸阅读
- 出处 — https://…
```

写作原则：先答后看（配合主动回忆）、示例驱动、给出来源。参考 `content/rust/ownership.md`。
